spring:
  cloud:
    config:
      server:
        native:
          search-locations: file:///C:/kiran/fincity/nocode-saas/configfiles
server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9999/eureka/
