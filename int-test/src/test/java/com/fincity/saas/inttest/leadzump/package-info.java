/**
 * Leadzump integration test scenarios.
 *
 * Each scenario class extends BaseIntegrationTest and uses ordered tests
 * to exercise a complete API workflow (authenticate → create → read → update → delete).
 */
package com.fincity.saas.inttest.leadzump;
